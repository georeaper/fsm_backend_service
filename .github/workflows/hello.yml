name: First Workflow

on:
  workflow_dispatch:
  # Temporarily disabled automatic build on dev push
  # push:
  #   branches:
  #     - dev

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Print message
        run: |
          echo "Hello from GitHub Actions 🚀"
          echo "Repo: $GITHUB_REPOSITORY"
          echo "Actor: $GITHUB_ACTOR"